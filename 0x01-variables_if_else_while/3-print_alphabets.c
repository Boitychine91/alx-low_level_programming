#include <stdio.h>

/**
 * main- prints alpha in lower cases
 *
 * Return:Always 0 (success)
 */

int main(void)
{
	char lc;

	for (lc = 'a'; <= 'z', lc++);
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
